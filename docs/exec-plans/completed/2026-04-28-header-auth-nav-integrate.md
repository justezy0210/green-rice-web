# Header Auth Nav Integration

## Goal

Make the Login / Sign Out action visually belong to the primary navigation instead
of appearing as a separate standout button.

## Completed

- [x] Moved auth action into the segmented nav group.
- [x] Reused the same nav item styling for Login / Sign Out.
- [x] Verified with `npm run type-check`, `npm run build`, `npm run lint`, and
  `git diff --check`.
