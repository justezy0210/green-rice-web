# SV Cultivar Toggle Filter

## Goal

Add cultivar toggle filters to `/sv` so users can inspect SVs carried by selected
cultivars, shared by selected cultivars, or specific to selected cultivars.

## Completed

- [x] Extended SV browse filtering with selected-cultivar modes.
- [x] Added cultivar toggle buttons and mode controls to the SV index page.
- [x] Verified with `npm run type-check`, `npm run build`, `npm run lint`,
  `git diff --check`, and `curl -I http://localhost:5173/sv`.
