# Header Navigation Style

## Goal

Make the primary navigation look less ad hoc and more consistent with the local
shadcn-style UI primitives.

## Completed

- [x] Reworked top-level nav links to use `buttonVariants`.
- [x] Grouped primary links in a restrained segmented nav container.
- [x] Updated dropdown and auth button sizing to align with the nav.
- [x] Verified with `npm run type-check`, `npm run build`, `npm run lint`,
  `git diff --check`, and `curl -I http://localhost:5173/about`.
