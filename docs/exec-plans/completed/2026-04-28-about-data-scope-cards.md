# About Data Scope Figure

## Goal

Replace the Data Scope table with a more presentation-friendly visual that shows
each database layer and its current release scope without forcing users to read a
wide table.

## Completed

- [x] Converted the Data Scope figure into compact layer cards with scope values.
- [x] Updated nearby About copy so it no longer refers to a table.
- [x] Removed the duplicate top summary strip and tightened the vertical spacing.
- [x] Normalized the gene annotation count to one user-facing value.
- [x] Verified with `npm run type-check`, `npm run build`, `npm run lint`, and
  `git diff --check`.
