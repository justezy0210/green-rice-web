# About Discovery Workflow Banner

## Goal

Add a dedicated About banner that explains how Discovery evidence is built, with
a compact workflow chart and clear positioning as review evidence rather than
causal proof.

## Completed

- [x] Added a Discovery workflow figure component.
- [x] Inserted a new About scene between Data Scope and Information Architecture.
- [x] Verified with `npm run type-check`, `npm run build`, `npm run lint`,
  `git diff --check`, and `curl -I http://localhost:5173/about`.
