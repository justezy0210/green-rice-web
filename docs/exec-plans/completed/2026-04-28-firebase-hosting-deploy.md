# Firebase Hosting Deploy

## Goal

Build the current frontend and deploy the updated site to Firebase Hosting.

## Completed

- [x] Confirmed hosting config targets `dist`.
- [x] Ran the production build with `npm run build`.
- [x] Deployed Firebase Hosting with `firebase deploy --only hosting`.
- [x] Verified deployed `/sv` and `/about` routes return HTTP 200.
